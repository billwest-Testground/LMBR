/**
 * useVendors — vendor roster hook.
 *
 * Purpose:  Fetches vendors for the current tenant with optional filters
 *           (region, commodity).
 * Inputs:   filter.
 * Outputs:  { vendors, isLoading, error }.
 * Agent/API: /api/vendors.
 * Imports:  @tanstack/react-query, @lmbr/types.
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

export function useVendors(): never {
  throw new Error('Not implemented');
}

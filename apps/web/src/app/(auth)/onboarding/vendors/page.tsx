/**
 * Onboarding — Vendors step.
 *
 * Purpose:  Imports or hand-enters the distributor's preferred vendors
 *           (mills, brokers, reloads) with region + commodity capability
 *           tags so the routing-agent can begin building vendor shortlists
 *           on the first real bid.
 * Inputs:   CSV upload or manual entry state.
 * Outputs:  Vendor-seed form JSX.
 * Agent/API: Supabase, @lmbr/agents routing-agent (preview).
 * Imports:  @lmbr/types (VendorSchema).
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

export default function OnboardingVendorsPage() {
  return <div>Not implemented: OnboardingVendorsPage</div>;
}

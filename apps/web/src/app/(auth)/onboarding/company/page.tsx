/**
 * Onboarding — Company step.
 *
 * Purpose:  Collects legal name, operating address, timezone, default
 *           margin percent, and manager-approval threshold so LMBR.ai can
 *           provision the tenant row in `public.companies` and seed
 *           sensible defaults for downstream workflows.
 * Inputs:   form state (client).
 * Outputs:  JSX form surface.
 * Agent/API: Supabase.
 * Imports:  @lmbr/types (CompanySchema), @lmbr/lib.
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

export default function OnboardingCompanyPage() {
  return <div>Not implemented: OnboardingCompanyPage</div>;
}

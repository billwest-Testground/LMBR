/**
 * Onboarding — Commodities step.
 *
 * Purpose:  Lets the Manager-Owner pick the species/grade/dimension combos
 *           they actually trade so the QA-agent and routing-agent can scope
 *           their recognition + vendor matching. Pulls candidates from
 *           @lmbr/config LUMBER_SPECIES, STANDARD_DIMENSIONS, LUMBER_GRADES.
 * Inputs:   selection state.
 * Outputs:  Commodity selection grid JSX.
 * Agent/API: Supabase write.
 * Imports:  @lmbr/config, @lmbr/types.
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

export default function OnboardingCommoditiesPage() {
  return <div>Not implemented: OnboardingCommoditiesPage</div>;
}

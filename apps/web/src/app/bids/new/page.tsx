/**
 * New bid — ingest surface (PDF / Excel / email / scan / manual).
 *
 * Purpose:  Starts a new LMBR.ai bid from any supported source. Hands off
 *           upload or text to /api/ingest which calls the ingest-agent.
 * Inputs:   file upload or pasted text.
 * Outputs:  Bid uploader surface JSX.
 * Agent/API: @lmbr/agents ingest-agent via /api/ingest.
 * Imports:  @lmbr/types (BidSource).
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

export default function NewBidPage() {
  return <div>Not implemented: NewBidPage</div>;
}

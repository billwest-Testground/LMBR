/**
 * Bid comparison page — vendor matrix + best-price selection.
 *
 * Purpose:  Surfaces the comparison-agent's head-to-head vendor pricing
 *           matrix. Traders/Buyers select the winning vendor per line.
 * Inputs:   params.bidId.
 * Outputs:  Comparison matrix JSX.
 * Agent/API: @lmbr/agents comparison-agent via /api/compare.
 * Imports:  @lmbr/types.
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

export default function ComparePage({
  params: _params,
}: {
  params: { bidId: string };
}) {
  return <div>Not implemented: ComparePage</div>;
}

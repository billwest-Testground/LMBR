/**
 * Vendors index — distributor's supplier roster.
 *
 * Purpose:  Lists every vendor (mill / broker / reload) for the tenant,
 *           with region + commodity tags and preferred-vendor flag used by
 *           the routing-agent for shortlist generation.
 * Inputs:   optional search params (region, commodity).
 * Outputs:  Vendor list JSX.
 * Agent/API: Supabase.
 * Imports:  @lmbr/types (Vendor).
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

export default function VendorsIndexPage() {
  return <div>Not implemented: VendorsIndexPage</div>;
}

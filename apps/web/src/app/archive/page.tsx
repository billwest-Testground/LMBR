/**
 * Archive page — historical won/lost bids.
 *
 * Purpose:  Lists archived LMBR.ai bids with search + filters. Sources the
 *           immutable `archive_entries` snapshots for audit and re-quote
 *           backfill.
 * Inputs:   search/filter params.
 * Outputs:  Archive list JSX.
 * Agent/API: Supabase.
 * Imports:  @lmbr/types.
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

export default function ArchivePage() {
  return <div>Not implemented: ArchivePage</div>;
}

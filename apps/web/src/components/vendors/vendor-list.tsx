/**
 * VendorList — supplier roster list.
 *
 * Purpose:  Lists every vendor with region/commodity/preferred tags.
 * Inputs:   { vendors: Vendor[] }.
 * Outputs:  JSX.
 * Agent/API: none.
 * Imports:  @lmbr/types (Vendor).
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

import type { Vendor } from '@lmbr/types';

export function VendorList(_props: { vendors: Vendor[] }) {
  return <div>Not implemented: VendorList</div>;
}

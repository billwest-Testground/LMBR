/**
 * VendorSelector — multi-select for the Buyer's solicitation fan-out.
 *
 * Purpose:  Lets the Buyer pick which vendors get a bid request. Seeds
 *           from routing-agent shortlist; Buyer can add/remove.
 * Inputs:   { candidateVendors, initiallySelectedIds }.
 * Outputs:  JSX + onChange callback.
 * Agent/API: routing-agent suggestions.
 * Imports:  @lmbr/types.
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

export function VendorSelector() {
  return <div>Not implemented: VendorSelector</div>;
}

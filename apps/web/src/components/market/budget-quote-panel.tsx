/**
 * BudgetQuotePanel — fast-path ballpark quote.
 *
 * Purpose:  Lets the Trader build a ballpark customer quote off market
 *           prices without running a vendor solicitation cycle.
 * Inputs:   { customerName, region?, lines[] }.
 * Outputs:  JSX + BudgetQuote result.
 * Agent/API: /api/budget-quote → market-agent.
 * Imports:  @lmbr/types (BudgetQuote), @lmbr/config.
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

export function BudgetQuotePanel() {
  return <div>Not implemented: BudgetQuotePanel</div>;
}

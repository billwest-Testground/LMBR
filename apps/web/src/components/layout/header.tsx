/**
 * Header — top bar for the LMBR.ai console.
 *
 * Purpose:  Renders the logo, global search (bids / vendors / quotes),
 *           role switcher, and user menu.
 * Inputs:   session user.
 * Outputs:  <header> JSX.
 * Agent/API: none.
 * Imports:  ./role-switcher.
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

export function Header() {
  return <div>Not implemented: Header</div>;
}

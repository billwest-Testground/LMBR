/**
 * BidList — scrollable list of bid cards.
 *
 * Purpose:  Renders the bid pipeline for the current filter set. Used by
 *           every dashboard variant.
 * Inputs:   { bids: Bid[] }.
 * Outputs:  JSX.
 * Agent/API: none.
 * Imports:  @lmbr/types (Bid), ./bid-card.
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

import type { Bid } from '@lmbr/types';

export function BidList(_props: { bids: Bid[] }) {
  return <div>Not implemented: BidList</div>;
}

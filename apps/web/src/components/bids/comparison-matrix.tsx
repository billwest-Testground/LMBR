/**
 * ComparisonMatrix — vendor × line-item price grid.
 *
 * Purpose:  Shows every received vendor's price for every consolidated
 *           line, highlights the winner per row, allows manual override.
 * Inputs:   { matrix, selections }.
 * Outputs:  JSX.
 * Agent/API: /api/compare.
 * Imports:  @lmbr/types.
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

export function ComparisonMatrix() {
  return <div>Not implemented: ComparisonMatrix</div>;
}

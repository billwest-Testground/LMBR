/**
 * QuotePreview — rendered quote preview (no vendor names).
 *
 * Purpose:  Inline preview of the final customer quote PDF. House/phase
 *           breakdown preserved; vendor names strictly excluded.
 * Inputs:   { quote: Quote, lineItems: LineItem[] }.
 * Outputs:  JSX.
 * Agent/API: /api/quote.
 * Imports:  @lmbr/types (Quote, LineItem).
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

export function QuotePreview() {
  return <div>Not implemented: QuotePreview</div>;
}

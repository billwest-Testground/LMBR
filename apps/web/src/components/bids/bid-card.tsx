/**
 * BidCard — single bid row (clickable).
 *
 * Purpose:  Shows customer, project, status chip, total BF, due date.
 * Inputs:   { bid: Bid }.
 * Outputs:  JSX.
 * Agent/API: none.
 * Imports:  @lmbr/types.
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

import type { Bid } from '@lmbr/types';

export function BidCard(_props: { bid: Bid }) {
  return <div>Not implemented: BidCard</div>;
}

/**
 * (tabs) layout — LMBR.ai mobile bottom tabs.
 *
 * Purpose:  Four-tab root for authenticated users: Dashboard, Bids, Buyers,
 *           Market. Icons via expo/vector-icons; labels adapt to active
 *           LMBR.ai role.
 * Inputs:   none.
 * Outputs:  <Tabs />.
 * Agent/API: none.
 * Imports:  expo-router.
 *
 * LMBR.ai — Enterprise AI bid automation for wholesale lumber distributors.
 * Built by Worklighter.
 */

import { Tabs } from 'expo-router';

export default function TabsLayout() {
  return <Tabs />;
}

{
  "name": "@lmbr/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and Zod schemas for LMBR.ai — enterprise AI bid automation for wholesale lumber distributors.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "echo \"@lmbr/types: lint stub\"",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}

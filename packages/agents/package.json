{
  "name": "@lmbr/agents",
  "version": "0.1.0",
  "private": true,
  "description": "LMBR.ai Claude-powered agents: ingest, QA, extraction, routing, pricing, comparison, market intel. Orchestrates the bid automation workflow.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "echo \"@lmbr/agents: lint stub\"",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "zod": "^3.23.0",
    "@lmbr/types": "workspace:*",
    "@lmbr/config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}

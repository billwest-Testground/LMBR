{
  "name": "@lmbr/config",
  "version": "0.1.0",
  "private": true,
  "description": "LMBR.ai shared configuration: lumber species, dimensions, grades, US regions, routing rules, tax rates. Consumed by agents and apps.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "echo \"@lmbr/config: lint stub\"",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
